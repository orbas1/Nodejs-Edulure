export const COMMUNITY_EVENT_PARTICIPANT_STATUSES = Object.freeze([
  'going',
  'interested',
  'waitlisted',
  'declined',
  'checked_in'
]);

export const COMMUNITY_EVENT_REMINDER_STATUSES = Object.freeze([
  'pending',
  'processing',
  'sent',
  'failed',
  'cancelled'
]);

export const COMMUNITY_EVENT_REMINDER_CHANNELS = Object.freeze([
  'email',
  'sms',
  'push',
  'in_app'
]);
